$( document ).ready(function() {
	console.log("teacher.js is alive");

	// var crntclass = $('#classes-menu option:selected').text().trim

	// $.post("/teachers/session", function(session) {
	// 	// console.log(session);
	// 	sessionStorage.setItem('sesh', JSON.stringify(session));
		

	// });
	// console.log(JSON.parse(sessionStorage.getItem('sesh')));

});

